<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="0dp"
    android:padding="0dp"
    tools:context=".ParkingLotMapActivity">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="68dp"
        android:layout_alignParentTop="true"
        android:background="@color/black"> <!-- Set your desired background color -->

        <!-- You can add header content here, such as title, logo, etc. -->


    </androidx.constraintlayout.widget.ConstraintLayout>

    /

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="657dp"
        android:layout_below="@+id/headerLayout"
        android:layout_marginTop="42dp"> <!-- Place below the header -->

        <GridLayout
            android:id="@+id/parkingLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_gravity="center"
            android:columnCount="8"
            android:orientation="horizontal"
            android:rowCount="5">

            <!-- Parking squares will be dynamically added here -->

        </GridLayout>
    </ScrollView>
</RelativeLayout>
